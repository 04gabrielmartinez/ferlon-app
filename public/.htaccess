RewriteEngine On

# Si existe archivo o carpeta física, se sirve directamente.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Todo lo demás va al front controller.
RewriteRule ^ index.php [QSA,L]
